spring:
  datasource:
    url: jdbc:h2:mem:fashionstore_test;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: fashionstore_db
    password: 123456789
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
    show-sql: false

app:
  jwt:
    secret: test-secret-key-that-is-long-enough-for-hs256-signing
    expiration: 86400000
